apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-project-sorting:null
spec:
  replicas: 1   # 👈 SINGLE pod only (no scaling)
  selector:
    matchLabels:
      app: demo-project-sorting:null
  template:
    metadata:
      labels:
        app: demo-project-sorting:null
    spec:
      containers:
        - name: demo-project-sorting:null
          image: demo-project-sorting:null
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: demo-project-sorting:null
spec:
  type: NodePort        # 👈 Simple + works everywhere
  selector:
    app: demo-project-sorting:null
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # 👈 Fixed port (no randomness)
